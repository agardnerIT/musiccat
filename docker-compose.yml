version: '3.8'

services:
  musiccat:
    build: .
    ports:
      - "6123:6123"
    volumes:
      - ./cert.pem:/app/cert.pem:ro
      - ./key.pem:/app/key.pem:ro
      - musiccat-data:/app
    restart: unless-stopped

volumes:
  musiccat-data:
